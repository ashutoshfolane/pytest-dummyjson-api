[pytest]
markers =
    smoke: Fast critical checks
    regression: Full regression suite
    contract: API schema / contract validation
    auth: Authorization-related tests
    flaky: Test failed initially but passed on retry (report-only)